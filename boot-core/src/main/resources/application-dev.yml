############################## 数据库配置 ######################################
spring:
  redis:
    database: 0
    host:
    password:
    port:
#    pool:
#      max-active: 50
#      max-idle: 8
#      min-idle: 0
#      max-wait: -1
    timeout: 0

  datasource:
      type: com.zaxxer.hikari.HikariDataSource
      url:
      username:
      password:
      hikari:
        idle-timeout: 10000
        maximum-pool-size: 10
        minimum-idle: 0

  jpa:
     show-sql: true
  profiles:
    include: info



################### 系统自定义配置  ###################
